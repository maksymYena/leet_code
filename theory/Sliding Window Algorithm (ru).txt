Алгоритм скользящего окна (Sliding Window Algorithm)

Скользящее окно (Sliding Window) — это оптимизированный способ обработки подмассивов (подстрок) фиксированной длины в массиве или строке.
Вместо того чтобы пересчитывать все заново при каждом сдвиге окна, мы уменьшаем и добавляем элементы по мере движения по массиву.

Ключевая идея
Мы используем два указателя (левая и правая границы окна), которые перемещаются по массиву, поддерживая текущее состояние окна.

Фиксированное окно (Fixed-size Window) — размер окна не меняется.
Переменное окно (Variable-size Window) — размер окна может изменяться.
В вашем случае (minimumRecolors), размер окна фиксирован и равен k, а задача — найти минимальное количество перекрашиваний.

Как работает скользящее окно?
Инициализация:

Начнем с первых k элементов и посчитаем количество нужных изменений (W → B).
Двигаем окно вправо:

Убираем элемент, выходящий из окна.
Добавляем новый элемент.
Обновляем минимальное значение.
Ответом будет минимальное количество замен среди всех окон.

Пример работы алгоритма
Задача: Найти минимальное количество W, которые нужно заменить в k подряд идущих символах.
python
Копировать
Редактировать
blocks = "WBBWWBBWBW"
k = 3
Шаг	Окно (k=3)	Кол-во W	min_recolors
1	"WBB"	1	1
2	"BBW"	1	1
3	"BWW"	2	1
4	"WWB"	2	1
5	"WBB"	1	1
6	"BBW"	1	1
7	"BWB"	1	1
🔹 Минимум перекрасок: 1

Временная сложность
Наивный подход (перебор всех k-подстрок): O(n*k), так как мы заново считаем символы для каждого окна.
Скользящее окно: O(n), потому что мы не пересчитываем все окно, а обновляем его за O(1) на каждом шаге.
Когда применять Sliding Window?
✔ Когда нужно найти оптимальное подмножество (подстроку) фиксированной длины
✔ Когда нужно искать максимум, минимум или сумму в окнах фиксированной длины
✔ Когда мы можем обновлять результат без полного пересчета

Другие примеры задач
Максимальная сумма k подряд идущих элементов

Вход: [2, 3, 4, 1, 5], k = 3
Выход: 10 ([4, 1, 5])
Поиск самой длинной подстроки без повторяющихся символов

Вход: "abcabcbb"
Выход: 3 ("abc")
Вывод
Скользящее окно уменьшает количество вычислений, поскольку вместо полного пересчета мы просто обновляем текущее состояние окна. 🚀